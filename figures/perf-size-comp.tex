\begin{tikzpicture}
	\begin{axis}[
			scale only axis, % ADD THIS TO BOTH
			width=0.85\textwidth,
			height=5.0cm, % Slightly reduced since the grid itself will now be this tall
			xlabel={Model Parameters (in millions)}, % (Keep your respective labels)
			ylabel={mIoU on Validation Dataset},
			grid=both,
			grid style={line width=.1pt, draw=Gray!10},
			major grid style={line width=.2pt, draw=Gray!30},
			xmin=-15, xmax=125, % (Or 165 for the second graph)
			ymin=0.80, ymax=0.88, % MAKE SURE BOTH SAY 0.80
			legend pos=south east,
			% Condensed legend to save plot space
			legend style={font=\fontsize{6pt}{7pt}\selectfont, fill=white, draw=black, nodes={inner sep=1pt}},
			enlarge x limits={rel=0.1},
			enlarge y limits={rel=0.1},
			title style={font=\bfseries},
			nodes near coords,
			point meta=explicit symbolic,
			every node near coord/.append style={
					font=\fontsize{6pt}{7pt}\selectfont, % Custom ultra-small font
					align=center, % Enables line breaks (\\) in labels
					inner sep=1.5pt,
				}
		]

		% Data for Baselines (Standard Models)
		\addplot[
			only marks,
			mark=square*,
			mark size=3pt,
			BalticBlue,
			every node near coord/.append style={xshift=-10pt, yshift=3pt}
		] table [meta=label] {
				x   y   label
				113.0 0.867 {DualSwinV2 + U-Net++ \\ (113M)}
				113.0 0.828 {DualSwinV2-S + Hybrid \\ (113M)}
			};
		\addlegendentry{DualSwinV2 Baselines}


		% Data for TinySwin + FPN
		\addplot[
			only marks,
			mark=triangle*,
			mark size=4pt,
			StormyTeal,
			every node near coord/.append style={anchor=south, xshift=10pt}
		] table [meta=label] {
				x   y   label
				33.0 0.837 {TinySwin + FPN \\ (33M)}
			};
		\addlegendentry{TinySwin + FPN}

		% Data for SS-JEPA (Label moved to the left)
		\addplot[
			only marks,
			mark=diamond*,
			mark size=4pt,
			Amaranth,
			every node near coord/.append style={anchor=east, xshift=14pt, yshift=8pt}
		] table [meta=label] {
				x   y   label
				20.0 0.865 {\textbf{SS-JEPA + UPerNet} \\ (20M)}
			};
		\addlegendentry{JEPA-based (Ours)}

		% Data for I-JEPA (Label moved to the right, plot forgotten for legend)
		\addplot[
			only marks,
			mark=diamond*,
			mark size=4pt,
			BrickRed,
			forget plot,
			every node near coord/.append style={anchor=north, yshift=-4pt}
		] table [meta=label] {
				x   y   label
				23.7 0.837 {I-JEPA + SegFormer \\ (23.7M)}
			};
	\end{axis}
\end{tikzpicture}

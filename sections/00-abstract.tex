% The expansion of autonomous exploration on Mars has unlocked unprecedented scientific frontiers, yet it requires navigating a landscape characterized by frequent landslides that drive geomorphological change by mass movement of rock and debris. This presents critical mechanical hazards to robotic assets. Mitigating these risks through accurate landslide prediction is therefore essential to safeguarding mission longevity and preventing catastrophic loss. While landslide prediction from multispectral images using traditional methods yields high accuracy, its generalizability is limited. In this work, we study the performance of self-supervised learning-based (SSL) image models, specifically trained as joint-embedding predictive architectures (JEPA) and develop a novel physics-aware image encoder, showing additional performance gains while maintaining the generalizability provided by SSL models. Experiments reveal that our physics-aware masking strategy captures long-range dependencies between spectral bands. We show that by learning cross-spectral latent correlations rather than raw pixel reconstructions, our model is significantly more robust to the sensor noise and varying lighting conditions commonly encountered in multispectral remote sensing data. We train JEPA encoders employing different masking strategies, and benchmark their downstream landslide segmentation performance on the MMLSv2 dataset. This work directly improves autonomous safety for planetary exploration and establishes a scalable framework for self-supervised learning in high-dimensional remote sensing and terrestrial disaster response.

% Topic (Pink): Autonomous Martian exploration relies heavily on geomorphological mapping to mitigate mechanical hazards to robotic assets.
% Motivation (Orange): While multispectral datasets have improved detection accuracy, traditional supervised models lack generalizability, and generative self-supervised approaches are computationally prohibitive and prone to learning trivial features through pixel reconstruction.
% Contribution (Yellow): In this work, we introduce Mars-JEPA, a Joint Embedding Predictive Architecture specifically designed for multispectral Martian landslide segmentation.
% Detail/Nuance (Green): Specifically, we propose a bovel Spatio-Spectral masking strategy that tokenizes channels independently, forcing the model to learn deep physical correlations between topography, thermophysics, and visible imagery in an abstract latent space.
% Evidence / Contribution 2 (Blue): On the MMLSv2 dataset, our best-performing SS-JEPA configuration achieves 0.865 mIoU, outperforming I-JEPA baselines by 2.8% and significantly exceeding the performance of single-model supervised backbones.
% Weaker result (Purple): While our single-model backbone trails the current state-of-the-art 5-fold ensemble by a marginal 0.2% mIoU, it does so with a 5.6
% ×
% ×
%  reduction in parameter count.
% Narrow impact (Red): Our results demonstrate that latent-space prediction is more robust to Martian sensor noise and varying lighting conditions than traditional generative SSL.
% Broad impact (Cyan): More broadly, this work establishes a scalable, parameter-efficient framework for high-dimensional remote sensing in resource-constrained planetary exploration environment.


% Autonomous Martian exploration relies heavily on geomorphological mapping to mitigate mechanical hazards to robotic assets.
% While multispectral datasets have improved detection accuracy, traditional supervised models lack generalizability, and generative self-supervised approaches are computationally prohibitive and prone to learning trivial features through pixel reconstruction.
% In this work, we introduce Mars-JEPA, a Joint Embedding Predictive Architecture specifically designed for multispectral Martian landslide segmentation.
% Specifically, we propose a bovel Spatio-Spectral masking strategy that tokenizes channels independently, forcing the model to learn deep physical correlations between topography, thermophysics, and visible imagery in an abstract latent space.
Autonomous Martian exploration relies heavily on geomorphological mapping to mitigate mechanical hazards to robotic assets. While multispectral datasets have improved detection accuracy, traditional supervised models lack generalizability, and generative self-supervised approaches are computationally prohibitive and prone to learning trivial features through pixel reconstruction. In this work, we introduce Spatio-Spectral JEPA (SS-JEPA), a Joint Embedding Predictive Architecture specifically designed for multispectral terrain feature representation learning. Specifically, we propose a novel Spatio-Spectral masking strategy that tokenizes channels independently, forcing the model to learn deep physical correlations between heterogeneous spectral bands, in an abstract latent space. On the MMLSv2 dataset, our best-performing SS-JEPA configuration achieves 0.865 mIoU, outperforming I-JEPA baselines by 2.8\% and significantly exceeding the performance of single-model supervised backbones. While our single-model backbone trails the current state-of-the-art 5-fold ensemble by a marginal 0.2\% mIoU, it does so with a \( 5.6 \times \) reduction in parameter count. Our results demonstrate that latent-space prediction is more robust to Martian sensor noise and varying lighting conditions than traditional generative SSL. More broadly, our work presents a scalable and efficient framework for learning semantic representations from multispectral remote sensing data for terrain feature analysis.
